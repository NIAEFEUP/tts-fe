import { makeStyles } from "@mui/styles";

export const timeTableStyles = makeStyles((theme) => ({
    container: {
        margin: "auto",
        padding: "2rem",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    },
}));
